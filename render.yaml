services:
  - type: web
    name: openmind-gateway
    runtime: python
    rootDir: openmind-subnet
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    healthCheckPath: /v1/health
    autoDeploy: true
