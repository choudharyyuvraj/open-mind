{
  "name": "openmind-memory",
  "version": "0.1.0",
  "description": "Persistent OpenMind memory behavior for Cursor: session-start recall, session-end save, and always-on memory guidance.",
  "author": {
    "name": "OpenMind"
  },
  "keywords": ["memory", "mcp", "openmind", "cursor"],
  "rules": "rules",
  "hooks": "hooks/hooks.json",
  "mcpServers": "mcp.json"
}

